// const Button = ({order}) => {
//     return (
//         <button className={`${order} ? 'proceed': 'cancel'`}>
//                         {${order} ? Proceed to Payment : Cancel Order};
//         </button>
//     )
// }

// export default Button
